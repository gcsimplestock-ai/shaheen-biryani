const navItems=document.querySelectorAll('.nav-item');const views=document.querySelectorAll('.view');const title=document.querySelector('#viewTitle');const labels={dashboard:'Dashboard',orders:'Orders',menu:'Menu',categories:'Categories',settings:'Settings'};function show(view){views.forEach(v=>v.classList.toggle('active',v.id===view));navItems.forEach(n=>n.classList.toggle('active',n.dataset.view===view));title.textContent=labels[view]||'Dashboard';history.replaceState(null,'',`#${view}`)}navItems.forEach(n=>n.addEventListener('click',()=>show(n.dataset.view)));document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.go)));const initial=location.hash.slice(1);show(labels[initial]?initial:'dashboard');